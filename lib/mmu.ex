defmodule MMU do
	def rb(addr) do
		# Read 8 bit byte from a given address
	end
	def rw(addr) do
		# Read 16 bit byte from a given address
	end
	def wb(addr, val) do
		# Write 8 bit value to a given address
	end
	def ww(addr, val) do
		# Write 16 bit word to a given address
	end
end